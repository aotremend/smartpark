spring:
  application:
    name: smartpark
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/smart_park
    username: root
    password: root
  data:
    jpa:
      repositories:
      enabled: true
server:
  port: 50100
  context-path: /smartpark